services:
  - type: web
    name: vigili-bot
    env: python
    plan: free
    buildCommand: |
      echo "🔄 Build in corso..."
      pip install -r requirements.txt
      python -c "print('✅ Build completata')"
    startCommand: |
      echo "🚀 Avvio Bot Vigili del Fuoco..."
      python bot.py
    envVars:
      - key: BOT_TOKEN
        value: "IL_TUO_BOT_TOKEN_QUI"
      - key: AUTO_REDEPLOY_ENABLED
        value: "false"  # Disabilitato di default
    healthCheckPath: /health
    autoDeploy: false  # IMPORTANTE: Disabilita auto-deploy da Git

databases:
  - name: vigili-db
    plan: free
    databaseName: vigili
    user: vigili_user
